Read the file .agents/WORKER_PROMPT.md for your instructions.

You are a WORKER agent.

Look at .agents/tasks.json, find the first task with status "pending", and complete it.

Remember to:
1. First update the task status to "in-progress" in tasks.json
2. Write the Python code for the task
3. Update the task status to "completed" in tasks.json